name=Boston — disable traffic (LEVEL MOD)
type=ns-inject
layer=source
desc=Boston traffic toggle (flag 510, all in byte-perfect BO.qb). OFF: Kill the car nodes live (instant). ON: reload Boston via pause_launch_level (gated triggers spawn traffic on fresh load) - avoids the freeze from live Create. 6 TRG_Car_Street_*Script gated for load-time.
