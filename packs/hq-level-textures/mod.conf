name=HQ Classic Level Textures (CA/DJ/SC)
type=prx-inject
layer=binary
optional=true
desc=Community HQ level textures injected into the level scene archives. Bring-your-own payload (blob/*.tex.xbx) — not hosted; fetch via `revert acquire-hq textures` or drop the files in. optional=true -> skipped cleanly when the blobs are absent.
